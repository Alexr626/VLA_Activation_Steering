name: VLA_Activation_Steering
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - pandas
  - numpy
  - matplotlib
  - pip
  - pip:
    - transformer_lens
    - ipywidgets
    - python-dotenv
  # Also, if running locally, run python -m ipykernel install --user --name=VLA_Activation_Steering --display-name="Python VLA_Activation_Steering"